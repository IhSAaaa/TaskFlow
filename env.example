# Database Configuration
POSTGRES_PASSWORD=your_secure_password_here

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here

# Ngrok Configuration
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
# Optional: Ngrok region (us, eu, ap, au, sa, jp, in)
# For Indonesia, use 'ap' (Asia Pacific) for best performance
NGROK_REGION=ap

# Ngrok URL (will be set automatically when ngrok starts)
# This will be updated dynamically by ngrok
NGROK_URL=https://your-ngrok-domain.ngrok-free.app

# Optional: Custom API URL for frontend (fallback)
# VITE_API_URL=http://your-custom-domain.com 